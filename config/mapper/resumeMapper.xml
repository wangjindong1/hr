<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iotek.ssm.dao.ResumeDao">

	<!-- <insert id="insertResume" parameterType="Resume">
		insert into 
		resumes(true_name,sex,age,education,phone_number,email,
		department_id,job_id,face,experience,hope_salary) 
		values(#{trueName},#{sex},#{age},#{education},#{phoneNumber},
		#{email},#{department.id},#{job.id},#{face},#{experience},
		#{hopeSalary}) 
	</insert>
	
	<delete id="deleteResumeById" parameterType="int">
		delete from resumes where id=#{id}
	</delete>
	
	<update id="updateResumeById" parameterType="Resume">
		update resumes 
		set 
		true_name=#{trueName},
		sex=#{sex},
		age=#{age},
		education=#{education},
		phone_number=#{phoneNumber},
		email=#{email},
		department_id=#{department.id},
		job_id=#{job.id},
		face=#{face},
		experience=#{experience},
		hope_salary=#{hopeSalary 
		where 
		id=#{id}
	</update>
	
	<select id="queryResumeById" parameterMap="int" resultType="Resume">
		select * from resumes where id=#{id}
	</select>
	
	<select id="queryAllResumes" resultType="Resume">
		select * from resumes
	</select> -->
</mapper>